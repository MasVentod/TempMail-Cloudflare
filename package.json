{
  "name": "cloudflare-telegram-tempmail-template",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --test-scheduled",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/router.js && node --check src/api.js && node --check src/telegram.js && node --check src/email.js && node --check src/db.js && node --check src/webhook.js && node --check src/api-doc.js && node --check src/utils.js && node --check src/constants.js",
    "db:migrate:local": "wrangler d1 migrations apply tempmail --local",
    "db:migrate:remote": "wrangler d1 migrations apply tempmail --remote"
  },
  "dependencies": {
    "postal-mime": "latest"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
